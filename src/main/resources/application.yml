#web的配置
server:
  port: 8080
#数据源的配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ibm_v6?useUnicode=true&characterEncoding=utf-8&serverTimezone=CST
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
#mybatis的配置
mybatis:
  config-location: classpath:MyBatis/Config/MybatisConfig.xml
  mapper-locations: classpath:MyBatis/Mapper/*.xml
